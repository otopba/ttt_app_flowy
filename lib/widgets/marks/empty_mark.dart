import 'package:flutter/material.dart';

class EmptyMark extends StatelessWidget {
  const EmptyMark({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
